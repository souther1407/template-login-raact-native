import { StyleSheet } from "react-native";
import { GLOBALS } from "../../../globals.js";
export const styles = StyleSheet.create({
  text: {
    fontSize: GLOBALS.fontBase,
  },
  subtitle: {
    fontSize: GLOBALS.fontBase + 8,
  },
  title: {
    fontSize: GLOBALS.fontBase + 16,
  },
  bold: {
    fontWeight: "bold",
  },
});
